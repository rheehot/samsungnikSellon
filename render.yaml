services:
  # Worker Service (주가 모니터링)
  - type: worker
    name: stock-monitor-worker
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m src.main
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: PYTHONPATH
        value: /opt/render/project/src
      - key: EMAIL_ENABLED
        value: true
      - key: EMAIL_SENDER
        value: rheehot@gmail.com
      - key: EMAIL_PASSWORD
        value: ncpb ukay lyrz ueaq
      - key: EMAIL_RECIPIENTS
        value: rheehot@gmail.com
